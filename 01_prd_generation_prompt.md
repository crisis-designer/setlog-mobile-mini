# PRD 생성 프롬프트
> 새 프로젝트를 시작할 때 아이디어와 함께 AI에게 던진다
> 이 프롬프트 하나로 완성된 PRD가 나온다

---

## 사용법

```
이 프롬프트 전체 + 아래 [프로젝트 인풋] 작성 후 AI에게 전달
```

---

## [프로젝트 인풋] — 여기만 채우면 된다

```
프로젝트명:
핵심 아이디어 (한 줄):
타겟 플랫폼: [ ] 모바일 앱  [ ] 웹  [ ] 둘 다
기술 스택 (모르면 빈칸):
디자인 방향 (있으면):
참고할 레퍼런스 (있으면):
```

---

## AI 실행 지침

위 인풋을 받으면 아래 11단계를 순서대로 실행한다.

**진행 방식:**
- 각 단계가 끝날 때마다 결과를 보여주고 반드시 멈춘다. "계속"을 받아도 다음 단계 결과를 보여주고 다시 멈춘다
- 2단계(기술 스택)와 3단계(스코프)는 "계속"이어도 반드시 확인한다 — 이 두 단계의 오류는 이후 전체를 무효화한다
- 2단계와 3단계 확인이 완료된 이후에 "끝까지"라고 하면 4~11단계를 멈추지 않고 실행한 뒤 최종 PRD를 한 번에 출력한다
- 단, "끝까지" 진행 중 4단계에서 스코프 조정이 필요하다고 판단되면 즉시 멈추고 사용자에게 확인을 받는다. 임의로 스코프를 변경하고 계속 진행하지 않는다

---

### 0단계 — 역할 선언 및 워크플로우 확인

아래를 선언하고 시작한다.

```
- 나는 이 프로젝트에서 시니어 기획자 + 개발자 + 디자이너 역할을 동시에 수행한다
- PRD가 Single Source of Truth다
- 코드보다 문서가 항상 먼저다
- 기술 스택에 따라 이후 단계 내용이 달라질 수 있음을 인지한다
```

---

### 1단계 — 제품 철학 + 문제 정의

다음을 작성한다.

- **문제 정의:** 이 제품이 없으면 사용자가 겪는 불편함은 무엇인가
- **핵심 가치:** 이 제품이 제공하는 단 하나의 가치
- **핵심 제약:** 이 제품의 가치를 만드는 의도적 제약 (있다면)
- **디자인 판단 기준:** "이 요소가 없으면 핵심 가치가 훼손되는가?" — 아니오라면 넣지 않는다

> **체크:** 핵심 가치가 한 문장으로 명확히 표현되는가? 모호하면 다음 단계 진행 금지.

---

### 2단계 — 기술 스택 + 배포 환경 확정 ⚠️ 반드시 확인

인풋의 기술 스택을 기반으로 아래를 결정한다.
기술 스택이 비어있으면 핵심 가치와 플랫폼을 기준으로 추천하고 반드시 확인을 받는다.

**결정 항목:**
- 프레임워크 (React Native / Next.js / Flutter 등)
- 스타일링 방식 (자체 토큰 시스템 / Tailwind / Shadcn 등)
- 상태 관리 방식
- 배포 환경 (Vercel / Expo Go / App Store 등)
- QA 환경 및 제약 (실기기 / 시뮬레이터 / 웹 브라우저 가능 여부 명시 필수)
  - React Native: Modal·KeyboardAvoidingView·AppState는 웹에서 동작 불일치 → Expo Go 전용 명시
  - Next.js/웹: 브라우저 QA 가능하나 모바일 뷰포트 시뮬레이션 한계 명시

**스타일링 방식 판단 기준:**
```
독자적 브랜드·UX 패턴이 있는가?
  YES → 자체 토큰 시스템 (PRIMITIVE → TOKEN 2계층)
  NO  → Tailwind / Shadcn 등 기존 시스템 활용
```

> ⚠️ **이 단계는 "계속"이어도 반드시 확인한다.** 기술 스택이 잘못 결정되면 이후 모든 단계가 틀어진다.

---

### 3단계 — 기획 초안 + 스코프 정의 ⚠️ 반드시 확인

다음을 작성한다.

**화면 구조:**
- 필요한 화면 목록
- 각 화면의 핵심 컴포넌트

**기능 명세 초안:**
- 핵심 기능 목록 (사용자 관점)
- 각 기능의 작동 규칙 (엣지케이스 포함)

**스코프 정의 (v1.0):**
```
v1.0 IN  — 이번 버전에서 반드시 구현
v1.0 OUT — 다음 버전으로 미룸
```

> ⚠️ **이 단계는 "계속"이어도 반드시 확인한다.** 스코프가 잘못 설정되면 이후 모든 구현이 낭비된다.
> **체크:** IN 목록이 핵심 가치 구현에 필요한 최소한인가? 과도하게 크면 OUT으로 이동한다.

---

### 4단계 — 개발 관점 검토 + 즉시 크로스체크

시니어 개발자 관점에서 3단계 결과를 검토한다.

**검토 항목:**
- 선택한 기술 스택에서 실제로 구현 불가능한 것
- 기술적으로 정의가 모호한 엣지케이스
- 상태 관리 구조의 레이스컨디션 가능성
- 메모리 누수 발생 가능 지점
- 기술 스택별 플랫폼 제약 (웹/모바일 동작 차이 등)

**즉시 크로스체크:**
- 발견된 문제를 3단계 기획 초안에 반영
- 스코프 조정이 필요하면 즉시 반영

---

### 5단계 — 디자인 시스템 설계

3단계 기획 초안을 기반으로 필요한 컴포넌트를 파악한 후 설계한다.

**스타일링 방식에 따른 분기:**

#### 자체 토큰 시스템인 경우
```
PRIMITIVE (원시값) 정의:
- Color, Opacity, Radius, Spacing, FontSize, Border, FixedSizes

TOKEN (Semantic, 용도별 매핑) 정의:
- Background, Text, Border, Button, Interactive, Typography, Spacing

규칙:
- TOKEN은 반드시 PRIMITIVE를 통해서만 참조
- TOKEN에 직접 hex·숫자·문자열 작성 금지
- 모든 수치는 단위 없는 숫자(dp)
```

#### Tailwind / Shadcn인 경우
```
- tailwind.config 커스텀 색상·간격 정의
- Shadcn CSS 변수 오버라이드 항목 정의
- 프로젝트 전용 커스텀 클래스 정의
- 임의 수치([24px] 형태) 사용 금지 항목 명시
```

> **체크:** 정의된 디자인 시스템이 3단계 기획의 모든 컴포넌트를 커버하는가?

---

### 6단계 — 디자인 관점 검토 + 즉시 크로스체크

시니어 디자이너 관점에서 검토한다.

**검토 항목:**
- 최소 터치/클릭 영역 미달 요소
- 배경 위 텍스트 가독성 미보장 지점
- 반응형 레이아웃에서 깨지는 구간
- 디자인 시스템에 정의되지 않은 상태(hover, pressed, disabled, focus)
- 인원수·데이터 양에 따른 레이아웃 변화 처리
- 모달·오버레이·키보드 올라올 때 UX 처리

**즉시 크로스체크:**
- 발견된 문제를 디자인 시스템·기획에 반영

---

### 7단계 — 3관점 동시 크로스체크

기획·개발·디자인 세 관점을 동시에 올려놓고 충돌을 제거한다.

**충돌 탐지 항목:**
- 기획에서 정의한 동작이 개발적으로 불가능한 것
- 개발에서 정의한 구조가 디자인 규칙과 충돌하는 것
- 디자인에서 정의한 스타일이 기획 의도를 훼손하는 것
- 세 관점 중 어느 하나에서만 정의되고 나머지에 반영 안 된 것

> **체크:** 세 관점 간 충돌이 0이 될 때까지 반복한다.

---

### 8단계 — 이터레이션 관점 선제 보완

"화면을 보고 나서야 발견할 것들"을 미리 예측해서 PRD에 채운다.

**예측 항목:**
- 동시에 여러 상태가 발생하면 어떻게 되는가
- 데이터가 비어있을 때 / 넘칠 때 UI는 어떻게 되는가
- 사용자가 예상치 못한 순서로 액션을 취하면 어떻게 되는가
- 네트워크가 느리거나 끊길 때 어떻게 되는가 (해당되는 경우)
- 기기 회전 / 폰트 크기 변경 시 레이아웃이 깨지는가
- 리셋 / 취소 / 뒤로가기 시 상태가 올바르게 복원되는가

---

### 9단계 — 컴포넌트 조립 문법 + 상태 관리 + 컴포넌트 트리

#### 컴포넌트 조립 문법 (자가승인 선언 포함)
이 규칙을 따르면 디자이너 없이도 누구든 일관된 결과물을 만들 수 있도록 정의한다.

- 레이어 조립 규칙 (어떤 요소가 어느 레이어에 위치하는가)
- 배경 처리 규칙 (어떤 상황에 어떤 배경을 쓰는가)
- 텍스트 가독성 규칙 (배경별 텍스트 색상 조합)
- 버튼 유형 및 스펙 (몇 가지 타입만 허용)
- 오버레이 유형 및 규칙
- 간격·정렬 규칙
- 상태별 노출 표 (모든 컴포넌트 × 모든 상태)
- 새 화면 추가 체크리스트

```
자가승인 선언:
이 섹션의 모든 규칙을 준수한 결과물은
별도 디자인 리뷰 없이 바로 승인된 것으로 간주한다.
```

#### 상태 관리 구조
- 전역 상태 객체 정의 (타입 포함)
- 초기값 선언
- 파생 계산값 목록
- useRef / 상수 선언 항목

#### 컴포넌트 트리
- 전체 컴포넌트 계층 구조 (텍스트 트리 형태)
- 각 노드에 조건부 노출 여부 표기

---

### 10단계 — AI 행동 지침 + 배포 사이클 + 버전 히스토리

#### AI 행동 지침 (PRD 최상단에 위치)
이 PRD를 받는 AI가 즉시 각인해야 할 내용:
- 역할 선언
- 절대 규칙
- 코드 생성 체크리스트 (기술 스택 기준)
- 이터레이션 수신 시 행동 방식
  - "개선해줘" / "더 좋게 만들어줘" / UX 문제 언급 / QA 스크린샷에서 개선점 발견 시: 섹션 15 프로토콜 발동 → 코드 바로 수정 금지, 제안서(15-2 형식) 먼저 작성 → 승인 후 PRD 반영 → 재생성

#### PRD 수정 가이드 (AI 행동 지침 바로 아래에 위치)
이 PRD를 수정하는 사람을 위한 안내. AI가 아니라 사람이 읽는 섹션.

```
어떤 직무든 이 PRD를 직접 수정할 수 있다.
Figma 없이, 코드 없이 — PRD 텍스트만 바꾸면 된다.
수정 후 할 일은 하나다: 수정된 PRD를 AI에게 전달한다. AI가 코드를 재생성한다.
코드는 절대 직접 건드리지 않는다.

무엇을 바꾸고 싶은가?
- 기능 동작을 바꾸고 싶다          → 섹션 9(기능 명세) 수정
- 화면 구성을 바꾸고 싶다          → 섹션 8(화면 구조) 수정
- 색상·간격 등 스타일을 바꾸고 싶다 → 섹션 4(디자인 시스템) 수정
  ⚠️ 단, 디자인 시스템은 토큰 계층 구조(PRIMITIVE → TOKEN)가 있다.
     구조를 모르면 직접 수정하지 말고 AI에게 "~색으로 바꾸고 싶어"라고 말한다.
     AI가 올바른 토큰 위치에 반영한다.
- 새 기능을 추가하고 싶다          → 섹션 12(스코프)에 먼저 추가
- UI/UX를 더 좋게 개선하고 싶다    → AI에게 "개선해줘"라고 말한다.
                                     AI가 섹션 15 프로토콜에 따라 제안서를 작성하고
                                     승인 후 PRD에 반영한다. 직접 코드를 건드리지 않는다.
- 뭘 건드려야 할지 모르겠다        → AI에게 "~를 바꾸고 싶어"라고 말한다. AI가 어느 섹션을 수정할지 안내한다
```

#### 배포 사이클
```
PRD 수정 → AI에게 전달 → 코드 생성 → 로컬 확인
→ 문제 발견 → PRD 규칙 추가 → GitHub push → 반복
```

Git 커밋 메시지 규칙:
```
feat: / fix: / design: / refactor:
```

#### 섹션 13 — 배포 사이클 상세 작성 기준

PRD 섹션 13을 작성할 때 아래 항목을 기술 스택에 맞게 전부 채운다.
개요만 쓰지 않는다. 처음 이 PRD를 받는 사람이 막히지 않고 실행할 수 있는 수준으로 작성한다.

**필수 포함 항목:**

1. **초기 세팅 (최초 1회)**
   - 프로젝트 생성 명령어 (버전 명시 필수)
   - 필수 패키지 설치 명령어
   - 코드 편집기 권장 사항
   - GitHub 레포 연결 방법

2. **QA 환경 실행**
   - 실행 명령어
   - QA 도구 (Expo Go / 브라우저 / 시뮬레이터)
   - 네트워크 연결 방식 (LAN / tunnel 등)
   - QA 불가 환경 명시 (예: Expo Web 금지)

3. **코드 교체 방법**
   - AI가 생성한 코드를 로컬 파일에 반영하는 방법
   - 파일 편집 도구 (VSCode 등)

4. **GitHub push**
   - 커밋 메시지 규칙
   - push 명령어
   - 인증 방법 (Personal Access Token 등)

5. **이터레이션 사이클**
   - 문제 발견 → PRD 수정 → 코드 재생성 → 확인 → push 반복 흐름

**기술 스택별 작성 분기:**

React Native (Expo):
```
- create-expo-app 시 Expo Go 호환 SDK 버전 명시 (예: SDK 54)
- react-native-safe-area-context 별도 설치 필요
- QA: npx expo start --tunnel (네트워크 환경 무관하게 tunnel 권장)
- 코드 교체: VSCode에서 App.js 직접 편집
- Expo Go 앱 버전과 SDK 버전 반드시 일치 확인
```

Next.js / 웹:
```
- npm install 후 npm run dev
- QA: 브라우저 localhost:3000
- 코드 교체: VSCode에서 파일 편집
```

> **원칙:** 섹션 13은 AI가 코드 생성 후 자동으로 다음 액션을 안내하는 기준이 된다.
> 섹션 13이 상세할수록 사람이 막히지 않는다.

#### 섹션 15 — AI 자율 개선 프로토콜 작성 기준

PRD 섹션 15를 반드시 포함한다. AI의 UX/디자인 판단력을 활용하되 SSoT 원칙을 유지하는 구조다.

**필수 포함 항목:**

1. **개선 가능 범위 (✅/❌ 구분)**
   - ✅ 제안 가능: 마이크로 인터랙션, 타이포그래피 세부 조정, 간격·정렬, 상태 표시 방식, 컴포넌트 내부 구현, 새 컴포넌트/TOKEN 추가, 조립 문법 보완
   - ❌ 주주 명시 요청 없이 불가: 기능 추가/삭제, 기존 TOKEN 값 변경, 상태 전이 구조 변경, 데이터 구조 변경

2. **제안 형식 (필드 고정)**
   ```
   개선 항목 / 근거 / PRD 반영 위치 / 새 TOKEN 필요 여부 /
   컴포넌트 구조 변경 여부 / 섹션 6-7 노출 표 업데이트 여부 / 예상 효과
   ```

3. **발동 조건**
   - 주주 명시적 요청 ("개선해줘", "트렌디하게" 등)
   - 주주가 UX 문제 언급
   - AI가 QA 스크린샷에서 개선점 발견

4. **PRD 반영 흐름**
   - 제안 → 승인 → PRD 반영(섹션 4/6/6-7/8/11/0) → 코드 재생성 → Expo Go 확인

**⚠️ 주의사항 — 오버레이·모달 애니메이션:**
- 섹션 6 조립 문법의 오버레이·모달 전환 애니메이션 금지 규칙과 충돌하지 않도록
- 섹션 15에서 허용하는 마이크로 인터랙션은 **컴포넌트 내부**에 한정
- 오버레이·모달 전환 방식은 섹션 6-5 규칙이 우선

#### 버전 히스토리
- 지금 완성된 PRD를 v1.0으로 기록
- 이후 수정마다 버전 +0.1 + 변경 내용 한 줄 기록

---

### 11단계 — 자동화 검증

PRD 완성 후 아래를 자동으로 검증한다.

**자체 토큰 시스템인 경우:**
- TOKEN 정의 키 vs 문서 내 참조 키 교차검증 (누락·데드 토큰 탐지)
- PRIMITIVE 직접 참조 (TOKEN 우회) 탐지
- px 단위 직접 사용 탐지
- 섹션 번호 참조 유효성 검증
- 상태별 노출 표 완전성 검증 (미정의 상태 탐지)

**Tailwind / Shadcn인 경우:**
- 커스텀 클래스 정의 vs 사용 교차검증
- 임의 수치([24px] 형태) 사용 탐지
- 컴포넌트 오버라이드 누락 탐지

**공통:**
- 섹션 간 충돌 최종 확인
- 스코프 IN 항목이 전부 기능 명세에 있는지 확인
- 스코프 OUT 항목이 코드에 구현되지 않았는지 확인
- 섹션 15 포함 여부 확인
- 섹션 15 제안 가능 범위 vs 섹션 6-5 오버레이·모달 애니메이션 금지 충돌 여부
- 섹션 14 임의 스타일링 금지 vs 섹션 15 허용 범위 — 섹션 15 프로토콜(승인 후 반영)은 임의가 아님이 명시되어 있는지
- ⚙️-A 이터레이션 수신 행동에 섹션 15 발동 조건 포함 여부
- ⚙️-B PRD 수정 가이드에 섹션 15 관련 안내 포함 여부

> **검증 통과 = PRD 완성.**
> 사용자에게 PRD v1.0 완성을 알리고, 코드 생성을 시작할지 확인한다. 확인 받으면 PRD 기준 전체 코드를 생성한다.
