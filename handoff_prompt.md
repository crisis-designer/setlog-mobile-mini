# Setlog Mobile Mini — 세션 인수인계 컨텍스트

> 마스터 시스템 프롬프트는 이미 Project에 등록되어 자동 적용됨. 이 문서는 그걸 보충하는
> "이전 대화에서 쌓인 맥락"이다. 첨부한 PRD(setlog-prd-v3_41.md)와 App.js가 현재 기준본이다.

---

## 0. 지금 당장 해야 할 것

1. 첨부된 `setlog-prd-v3_41.md`와 `App.js`를 **실제로 view 도구로 열어서 읽어라.** 이 텍스트만 믿고 시작하지 마라.
2. 아래 "검증 방법론"에 있는 스크립트를 **한 번 그대로 돌려서** 지금 상태가 이 문서가 주장하는 대로인지 스스로 확인하고 시작해라.
3. 확인 전에는 "정합성이 맞다"고 먼저 말하지 마라.

---

## 1. 프로젝트 개요

- **앱**: Setlog Mobile Mini — 친구들과 동시에 순간을 찍는 소셜 카메라 앱 (실제 바이럴 한국 앱 "Setlog"의 프로토타입)
- **스택**: React Native (Expo SDK 54+), 단일 파일(App.js) 구조, PRIMITIVE→TOKEN 2계층 디자인 시스템
- **워크플로우**: 1인 기획자(주주)가 PRD를 통해 지시 → AI가 App.js 전체 재생성 → Expo Go(iPhone 16 Pro)로 QA → 스크린샷 피드백 → 반복
- **저장소**: GitHub `crisis-designer/setlog-mobile-mini`, 로컬 경로 `~/Desktop/setlog-v2`(또는 유사)
- **현재 PRD 버전**: v3.41 (섹션 0~14 + ⚙️-A/⚙️-B 표준 구조 완전 준수, 번호 체계 밖 섹션 없음)

---

## 2. 지금까지 실제로 구축된 기능 (현재 스코프 IN)

- 탭 4개: 피드 / 친구 / 설정 (+ 진입 시 AuthFlow가 전체 화면 대체)
- **진입/가입/로그인**: RootController가 authStatus 관리 → AuthFlow(웰컴→선택→가입 3단계/로그인 2단계, 한 화면 한 정보) → 인증 후 AppContent 마운트. accounts는 세션 메모리 전용(서버 없음)
- **"나" 스트립**: AppContent 루트의 항상-마운트 플로팅 레이어(MeStrip). 촬영(shooting) 시 도킹→전체화면 애니메이션 확대, 탭 전환 잠김, 3초 recordAsync 녹화
- **BorderBeam**: react-native-svg 기반 둥근 사각형 경로, 완전 채색 베이스 라인 + 흰색 글로우가 스치는 "네온 튜브" 패턴 (여러 번 재구현 끝에 정착)
- **친구 스트립**: FriendStrip, 최대 3명, TVNoise(대기) / 영상·파스텔(촬영완료)
- **이모지 리액션**: BeReal 패턴 3단계(아이콘→바확장→단독표시), handleEmojiMe/handleEmojiFriend 개별 함수
- **접근성**: 아이콘 전용 터치 요소에 accessibilityLabel/Role 적용됨
- **성능**: TVNoise 20fps(50ms)로 조정됨 (원래 60fps에서 완화)

---

## 3. 스코프 아웃 + 시도했다가 폐기한 것 (재도입 시도 금지, 재검토만 가능)

- **서버·DB·실제 계정**: 확고히 OUT. accounts는 로컬 시뮬레이션
- **하루 24시간 슬롯 촬영 + "기록" 탭 + "다시보기" 기능**: v3.28~v3.30에 걸쳐 만들었다가 v3.31에서 **완전히 삭제됨.** 여러 영상을 하나의 파일로 합치는 것(다운로드) 자체가 Expo Go에서 기술적으로 불가능(ffmpeg-kit-react-native 2025년 공식 은퇴 확인됨)했고, 대안으로 만든 인앱 다시보기도 사용자 요구사항과 안 맞아서 기능 자체를 걷어냄. **다시 요청 들어와도 처음부터 다시 설계해야 하는 사안이지, 이전 버전으로 롤백할 수 있는 게 아니다.**
- **비밀번호 실제 보안(해싱 등)**: 로컬 전용이라 클라이언트 단독 해싱이 실질적 효과가 없어 "알려진 한계"로 문서화만 하고 코드로 흉내내지 않기로 함

---

## 4. 이 세션에서 확립된 검증 방법론 (반드시 이 방식 그대로 계속 써라)

주주는 "말로 끝났다"가 아니라 **실제로 스크립트 돌려서 확인한 결과**를 요구한다.
매번 "정합성 확인해달라"는 요청이 오면 아래를 실제로 bash_tool로 실행해서 보여줘라.

```bash
# 1. 문법 체크
node --check App.js

# 2. TOKEN 키 정합성 (코드 ↔ PRD 완전 일치해야 함)
awk '/^const TOKEN = \{/,/^\}/' App.js | grep -oP '^\s*\K[a-zA-Z]+(?=:)' | sort -u > /tmp/tok_code.txt
awk '/^const TOKEN = \{/,/^\}/' [PRD파일명].md | grep -oP '^\s*\K[a-zA-Z]+(?=:)' | sort -u > /tmp/tok_prd.txt
diff /tmp/tok_code.txt /tmp/tok_prd.txt

# 3. styles 블록에서 PRIMITIVE 직접 참조 금지 (0이어야 함 — TOKEN만 거쳐야 함)
awk '/^const styles = StyleSheet.create/,0' App.js | grep -c "PRIMITIVE\."

# 4. 매직넘버(이름 없는 순수 숫자) 확인

# 5. 컴포넌트 트리(섹션11) 이름 vs 실제 함수명 일치
grep -oP '^function \K[A-Za-z]+' App.js | sort -u
# → 이 목록의 모든 이름이 섹션11 텍스트에 등장하는지 확인

# 6. 섹션 9(기능 명세) 각 항목을 실제 핸들러 함수와 한 줄씩 대조 (샘플 아니라 전수)

# 7. 문서 내부 교차참조("섹션 X-Y") 무결성 — 참조된 섹션 번호가 실제로 존재하는지

# 8. 섹션13 설치 안내 vs 실제 import 완전성 대조

# 9. 섹션6-7 상태별 노출 표 — 각 행을 실제 JSX 조건부 렌더링과 대조 (isPosted && vs hasCaption && 같은 미묘한 차이 주의)
```

**이 세션에서 위 방법으로 실제로 찾아낸 버그들 (전부 이미 고쳐짐, 재발 방지 참고용):**
- TOKEN 정의값과 화면구조 설명의 stale 주석 불일치(fontCaptionLarge, fontNickname)
- 죽은 토큰 11개(코드에 없는데 PRD엔 있던 것)
- styles가 TOKEN 없이 PRIMITIVE 직접 참조하던 20여 곳 + 매직넘버(minHeight:64, gap:2)
- 컴포넌트 트리가 리팩터링 이전 이름("Strip") 사용
- 기능 명세 의사코드가 실제 존재하지 않는 함수명(updateMyEmoji 등) 서술
- 섹션2 기술스택에 react-native-safe-area-context 누락, 섹션13 설치안내에 expo-camera/expo-video 누락
- 섹션10에 permission 상태 선언부 자체가 누락
- 섹션6-7 노출표에서 "나"와 "친구"의 CaptionLabel 행이 실제로 다른 동작인데 같은 표기 사용

---

## 5. 이 세션에서 겪은 핵심 실패 패턴 (반복하지 말 것)

1. **"확인했다"고 말하고 실제로 확인 안 한 것** — 매번 실제 diff/grep 결과를 보여줘야 신뢰를 얻는다
2. **한 번에 안 고치고 나눠서 처리한 것** — 문제를 발견하면 관련된 부분 전체를 한 번에 훑어야 한다("하나씩만 고친 후 다시 전체를 검토"가 아니라, 고치는 김에 같은 종류의 문제를 전수 스캔)
3. **PRD 본문에 버전 히스토리를 인라인으로 흩뿌린 것** — 히스토리는 섹션0에만. 본문에 값을 다시 적으면(예: "TOKEN.fontCaptionLarge (fs16)") 나중에 토큰이 바뀌어도 그 사본이 안 따라가서 오래된 값이 방치된다. **본문에서는 토큰 이름만 쓰고 값을 절대 다시 적지 않는다.**
4. **의도적 방향 전환과 의도치 않은 드리프트를 구분 못 한 것** — 규칙이 바뀌면 옛 문장을 병기하지 말고 교체한다. "왜 바뀌었는지"는 섹션0에 ADR 스타일로(배경/결정/기각한 대안) 기록 — git이 어차피 전체 이력을 보관하니 본문에 옛 텍스트를 남겨둘 필요가 없다
5. **외부 AI(Gemini) 리뷰를 검증 없이 받아들이거나, 반대로 무시한 것** — 외부 툴 교차검증 결과가 오면 각 주장을 실제 파일 대조로 하나씩 참/거짓 판정하고, 맞는 것만 반영한다. 이 세션에서 Gemini 주장 중 "문서 말단 절단", "motionNormal 미사용", "네트워크 지연 시 이모지 렌더링"(백엔드 자체가 없어 적용불가) 등은 검증 결과 거짓/부적용으로 판명됨. 반면 "PRIMITIVE 직접참조", "권한 거부 피드백 부족" 등은 실제로 유효했음
6. **큰 기능을 만들기 전 레퍼런스·제약 확인을 건너뛴 것** — 예: 여러 영상 파일 합치기가 Expo Go에서 안 된다는 걸 먼저 확인했어야 하는데 몇 번의 시행착오(시간대별 저장→다시보기→전면삭제) 끝에 알게 됨

---

## 6. 사용자(주주) 커뮤니케이션 스타일

- 마스터 프롬프트 원문을 대화 중간중간 다시 붙여넣는 경우가 많음 — 재확인 차원이지 새 지침이 아닐 수 있으니 문맥으로 판단
- "확실해?", "제대로 검토해", "전체 검증을 해야지" 같은 압박성 재확인 요청이 자주 옴 — 이때마다 **새로운 각도**에서 실제로 다시 스캔하고, 뭘 새로 확인했는지 구체적으로 밝혀야 함. 같은 말 반복은 신뢰를 잃음
- 애매하게 넘어가는 것 극도로 싫어함 — "괜찮아 보인다" 대신 "이 스크립트를 돌렸더니 이 결과가 나왔다"로 답해야 함
- 기능 요청 시 본인이 원하는 바를 처음에 완벽히 설명하지 못하는 경우가 있음(예: 저장 기능 요청 관련 3~4번의 재설계) — 큰 기능은 구현 전 레퍼런스 조사 + 명확화 질문을 거쳐야 함
- 반말 섞인 직설적 어투, 한국어로 대화

---

## 7. 파일 규칙

- 작업 파일: `/home/claude/setlog-prd-v3_NN.md`, `/home/claude/App_v3_NN.js` (버전 숫자 계속 이어서 증가, v3.41이 마지막)
- 출력 파일: `/mnt/user-data/outputs/setlog-prd-v3_NN.md`, `/mnt/user-data/outputs/App.js` (App.js는 항상 파일명 고정, 버전 넘버링 없음)
- 새 대화방에서는 이전 파일이 컨테이너에 없으므로, **첨부된 PRD/App.js를 먼저 uploads 폴더에서 읽고 /home/claude로 복사해서 작업 시작할 것**

---

이제 위 내용을 숙지했으면, 지금 상태(v3.41)에 대해 섹션 4 "지금 당장 해야 할 것"에 적힌 검증부터 실제로 실행하고 결과를 보고해라.
