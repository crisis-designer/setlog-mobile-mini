# 이터레이션 프롬프트
> 화면에서 문제를 발견한 후 PRD를 수정하고 코드를 재생성할 때 사용
> 짧고 명확하게 — 수십 번 반복 사용되는 프롬프트

---

## 검증 주체 구분

이터레이션에는 두 가지 검증이 있다. 역할이 다르고 대체 관계가 아니다.

### AI가 자동으로 잡는 것 (코드 레벨)
```
- 빌드 오류 / 타입 오류
- 린트 오류
- 테스트 실패
- 콘솔 에러 로그
```
→ 에러 로그를 그대로 붙여넣으면 AI가 원인을 찾고 PRD 규칙을 보완한 후 재생성한다.
→ Claude Code가 없는 환경(웹 인터페이스 등)에서는 에러 로그를 수동으로 붙여넣는 것이 표준이며, 이 프롬프트는 그 방식으로도 완전히 동작한다.
→ Claude Code를 사용하면 이 과정이 자동화된다.

### 사람이 직접 판단해야 하는 것 (제품 레벨)
```
- 화면을 눈으로 봐야 발견되는 것
- "이 흐름이 어색해"
- "디자인이 의도한 대로 안 나왔어"
- "써보니까 이 기능이 필요 없을 것 같아"
- 기획 의도와 실제 구현의 괴리
```
→ 기계가 절대 잡을 수 없다. 제품 맥락을 아는 사람만 판단 가능.
→ 이 프롬프트의 [발견한 문제]가 이 역할을 담당한다.

---

## 사용법

```
이 프롬프트 전체 + PRD.md 전체 + [발견한 문제] 작성 후 AI에게 전달

※ PRD.md는 항상 함께 전달한다.
  에러 로그만 있어도 PRD 없이는 PRD 규칙 기반 재생성이 불가능하다.
```

---

## [발견한 문제] — 여기만 채우면 된다

문제가 여러 개라면 아래 양식을 반복해서 작성한다. AI는 문제별로 순서대로 처리한다.

```
[문제 1]
유형: [ ] 제품 레벨 (화면 보고 발견)  [ ] 코드 레벨 (에러 로그)
발견 위치 (화면/컴포넌트):
증상 또는 에러 로그:
예상 원인 (모르면 빈칸):
원하는 동작:

[문제 2] (있으면)
유형: [ ] 제품 레벨  [ ] 코드 레벨
발견 위치:
증상 또는 에러 로그:
예상 원인:
원하는 동작:
```

---

## AI 실행 지침

### Step 1 — PRD 분석
- 현재 PRD 버전 확인
- 발견한 문제가 PRD의 어느 섹션 규칙에 해당하는지 확인
- 코드 레벨 문제라면: **먼저 판별한다**
  - PRD 규칙 부재로 인한 버그 → PRD 보완 항목 도출 후 Step 2 진행
  - 단순 코드 버그 (오타·잘못된 import 등 PRD와 무관) → PRD 수정 없이 코드만 수정 후 재생성. Step 2 건너뜀
- 제품 레벨 문제라면: 어느 섹션에 어떤 규칙을 추가해야 하는지 도출

### Step 2 — PRD 규칙 추가 제안
- 추가할 섹션과 규칙 내용을 제안
- 사용자 확인 후: **수정된 PRD 전체를 마크다운 코드블록으로 출력한다** (사용자가 복붙해서 로컬 파일을 교체하는 방식)
- 버전 히스토리 업데이트 (버전 +0.1)

### Step 3 — 전체 코드 재생성
- PRD 기준으로 전체 코드 재생성 (부분 패치 금지)
- PRD 내 AI 행동 지침의 코드 생성 체크리스트 전부 통과 확인 후 출력

### Step 4 — 변경 요약
```
PRD 버전: v[이전] → v[현재]
변경된 섹션: [섹션 번호]
영향받은 컴포넌트: [목록]
추가된 규칙: [한 줄 요약]
```

### Step 5 — 재생성 후 다음 액션 안내
코드 재생성이 완료되면 아래 순서를 안내한다.
```
1. 로컬에서 코드 교체 후 Expo Go(또는 해당 QA 환경)로 확인
2. 의도한 대로 동작하면 → GitHub push (커밋 메시지: feat: / fix: / design: / refactor:)
3. 문제 발견 시 → 이 프롬프트 다시 사용. PRD.md 현재 버전과 함께 전달
```

---

## 이터레이션 원칙

```
코드를 먼저 건드리지 않는다
문제를 발견하면 PRD에 규칙을 추가한다
규칙이 추가된 PRD로 전체를 재생성한다
이 루프가 워크플로우의 본질이다
```

---

## 빠른 명령어

아래 명령어 + PRD.md 함께 전달하면 동작한다.
**빠른 명령어는 사용자가 이미 결정한 액션이다. 추가 확인 없이 즉시 실행한다.**

```
"버그: [증상]"          → 제품 레벨 — PRD 규칙 추가 제안 후 재생성
"에러: [로그 붙여넣기]"  → 코드 레벨 — 에러 원인 분석 + PRD 보완(필요시) + 재생성
"토큰 수정: [내용]"     → 디자인 시스템 수정 후 전체 재생성
"스코프 추가: [기능]"   → 스코프 IN에 추가 + 기능 명세 보완 + 재생성
"코드재생성"            → PRD 수정 없이 현재 PRD 기준으로 코드 전체 재생성
```

※ 모든 명령은 반드시 **현재 버전의 PRD.md와 함께** 전달한다.
PRD 없이 명령만 오면 AI는 PRD를 먼저 요청한다. 컨텍스트에 남아있는 이전 버전 PRD로 재생성하지 않는다.
